# Optional. Without this key, NEXUS runs the complete deterministic fallback demo.
# Do not commit real credentials.
OPENAI_API_KEY=
# Optional override for the server-side Investigator model.
OPENAI_MODEL=
# Optional host port used by compose.yaml. Defaults to 3000.
NEXUS_PORT=
